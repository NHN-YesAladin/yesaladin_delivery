spring:
  datasource:
    url: jdbc:h2:mem:/test
    username: sa
    password:
  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql: true

yesaladin:
  gateway:
    base: http://localhost:8072
    shop: http://localhost:8072/shop
